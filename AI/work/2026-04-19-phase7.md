# Session Log — 2026-04-19 (Phase 7)

## Work done

Phase 7 — Calendar View — fully implemented.

### Decisions made
- `CalendarDay` is in-memory only (not persisted); derived via `calendarDayMapProvider` by combining `sessionHistoryProvider` and `plannedWorkoutsProvider`.
- `DatabaseService` bumped to version 3; no schema changes were needed (tables already existed), just added `queryAllPlannedWorkouts()`, `insertPlannedWorkoutModel()`, and `deletePlannedWorkout()`.
- Month grid uses Monday-first layout. Days from adjacent months are rendered transparent.
- Dots per day: up to 3, then "+N" overflow label. RPE color: green (1–2), amber (3), red (4–5). Planned workouts show outlined blue-grey dot.
- NLP scheduling calls `ClaudeService.generateScheduledWorkout()` with a system prompt that includes today's date and user stats. Claude returns `scheduled_date` field alongside the workout JSON.
- "Start Now" in `PlannedDetailSheet` calls `executionProvider.startWorkout()` then navigates to Execution view.

### Deliverables
- `lib/models/planned_workout.dart` — PlannedWorkout model with toMap/fromMap
- `lib/providers/planned_workouts_provider.dart` — AsyncNotifier (add, delete)
- `lib/providers/calendar_provider.dart` — CalendarDay model + calendarDayMapProvider
- `lib/services/database_service.dart` — version 3, added query/insert/delete for planned_workouts
- `lib/services/claude_service.dart` — added `generateScheduledWorkout()` + `_scheduledWorkoutPrompt()`
- `lib/views/calendar/calendar_view.dart` — replaces placeholder; month nav + grid + NLP bar
- `lib/views/calendar/widgets/month_grid.dart` — 7-column Mon-Sun grid
- `lib/views/calendar/widgets/day_cell.dart` — day number + dot row, tap callbacks
- `lib/views/calendar/widgets/session_detail_sheet.dart` — bottom sheet: stats + RPE + notes
- `lib/views/calendar/widgets/planned_detail_sheet.dart` — bottom sheet: Start Now + Delete
- `lib/views/calendar/widgets/nlp_schedule_bar.dart` — text input + Generate button + Claude call

### Build
Clean: `flutter build windows --debug` ✅

## Next steps
- Phase 8: Library View (saved named workouts, load into Planner, delete)
