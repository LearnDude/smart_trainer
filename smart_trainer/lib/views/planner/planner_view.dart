import 'package:flutter/material.dart';

class PlannerView extends StatelessWidget {
  const PlannerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Workout Planner — Phase 4', style: TextStyle(fontSize: 24)),
    );
  }
}
