import 'package:flutter/material.dart';

class PostSessionView extends StatelessWidget {
  const PostSessionView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Post-Session — Phase 6', style: TextStyle(fontSize: 24)),
    );
  }
}
